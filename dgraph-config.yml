# Dgraph configuration for standalone deployment

datadir: /dgraph/data
bindall: true

# HTTP & GRPC ports
port_offset: 0
grpc_port: 9080
http_port: 8080

# Alpha configuration
alpha:
  lru_mb: 2048

# Security settings (adjust as needed)
whitelist: 32.219.125.112/32

# Logging
logtostderr: true
v: 2

# Performance tuning for cloud deployments
badger:
  compression: snappy
  numgoroutines: 16

graphql:
  lambda_url: "https://dgraph-lambda.onrender.com/graphql-worker"